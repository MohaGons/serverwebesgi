[Definition]
failregex = ^ \[error\] \d+#\d+: \*\d+ user "(?:\S+|.*?)" (?:password mismatch|was not found in ".*?") while processing request: "GET .*?"(, client: <HOST>).*$
            ^ \[error\] \d+#\d+: \*\d+ no user/password was provided for basic authentication, client: <HOST>.*$
ignoreregex =